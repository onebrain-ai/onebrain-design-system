// GENERATED by tokens/build.js from tokens.json — SwiftUI tokens (iOS app surface).
// Drop into an iOS target. Color(hex:) accepts #RRGGBB or #RRGGBBAA.
import SwiftUI

public enum OneBrain {
    public enum ColorsDark {
        static let bg = Color(hex: "#050507")
        static let bgDeep = Color(hex: "#020204")
        static let surface = Color(hex: "#0a0a12")
        static let surface2 = Color(hex: "#08080e")
        static let border = Color(hex: "#ffffff14")
        static let border2 = Color(hex: "#ffffff0f")
        static let text = Color(hex: "#f0f0f2")
        static let muted = Color(hex: "#a1a1aa")
        static let faint = Color(hex: "#ffffff9e")
        static let ghost = Color(hex: "#ffffff2e")
        static let white = Color(hex: "#ffffff")
        static let accent = Color(hex: "#bc13fe")
        static let accent2 = Color(hex: "#00f3ff")
        static let accent3 = Color(hex: "#ff2d92")
        static let accent4 = Color(hex: "#ffb000")
        static let vault = Color(hex: "#7c3aed")
        static let vault2 = Color(hex: "#a78bfa")
        static let harness = Color(hex: "#00cce0")
        static let harness2 = Color(hex: "#00646e")
        static let loop = Color(hex: "#a8d000")
        static let loop2 = Color(hex: "#d4ff45")
        static let success = Color(hex: "#a8d000")
        static let info = Color(hex: "#00f3ff")
        static let warning = Color(hex: "#ffb000")
        static let danger = Color(hex: "#ff2d92")
    }
    public enum ColorsLight {
        static let bg = Color(hex: "#f4f4f7")
        static let bgDeep = Color(hex: "#ffffff")
        static let surface = Color(hex: "#ffffff")
        static let surface2 = Color(hex: "#fafafe")
        static let border = Color(hex: "#0808101f")
        static let border2 = Color(hex: "#08081014")
        static let text = Color(hex: "#0a0a12")
        static let muted = Color(hex: "#52525b")
        static let faint = Color(hex: "#0a0a1299")
        static let ghost = Color(hex: "#0a0a1259")
        static let white = Color(hex: "#0a0a12")
        static let accent = Color(hex: "#9500c7")
        static let accent2 = Color(hex: "#007a90")
        static let accent3 = Color(hex: "#c2186a")
        static let accent4 = Color(hex: "#9a6400")
        static let vault = Color(hex: "#7c3aed")
        static let vault2 = Color(hex: "#a78bfa")
        static let harness = Color(hex: "#00cce0")
        static let harness2 = Color(hex: "#00646e")
        static let loop = Color(hex: "#a8d000")
        static let loop2 = Color(hex: "#d4ff45")
        static let success = Color(hex: "#5f7d00")
        static let info = Color(hex: "#007a90")
        static let warning = Color(hex: "#9a6400")
        static let danger = Color(hex: "#c2186a")
    }
    public enum Space {
        static let s0: CGFloat = 0
        static let s1: CGFloat = 4
        static let s2: CGFloat = 8
        static let s3: CGFloat = 12
        static let s4: CGFloat = 16
        static let s5: CGFloat = 24
        static let s6: CGFloat = 32
        static let s7: CGFloat = 40
        static let s8: CGFloat = 64
        static let s9: CGFloat = 96
    }
    public enum Radius {
        static let 0: CGFloat = 0
        static let xs: CGFloat = 2
        static let sm: CGFloat = 4
        static let pill: CGFloat = 999
    }
    public enum FontSize {
        static let eyebrow: CGFloat = 10
        static let xs: CGFloat = 11
        static let sm: CGFloat = 13
        static let base: CGFloat = 16
        static let md: CGFloat = 17
        static let lg: CGFloat = 20
        static let xl: CGFloat = 24
        static let xxl: CGFloat = 32
        static let h2: CGFloat = 56
        static let hero: CGFloat = 72
    }
}

public extension Color {
    init(hex: String) {
        var s = hex.trimmingCharacters(in: CharacterSet(charactersIn: "#"))
        if s.count == 6 { s += "FF" }
        var n: UInt64 = 0
        Scanner(string: s).scanHexInt64(&n)
        let r = Double((n & 0xFF000000) >> 24) / 255
        let g = Double((n & 0x00FF0000) >> 16) / 255
        let b = Double((n & 0x0000FF00) >> 8) / 255
        let a = Double(n & 0x000000FF) / 255
        self.init(.sRGB, red: r, green: g, blue: b, opacity: a)
    }
}
